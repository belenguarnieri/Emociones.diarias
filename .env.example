# Clave pública, se usa en el build del frontend (con prefijo VITE_).
VITE_VAPID_PUBLIC_KEY=

# Estas tres son privadas: se configuran SOLO como variables de entorno
# en Vercel, nunca se suben a GitHub.
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# PUSH_SUBSCRIPTION=
# CRON_SECRET=
